<?xml version="1.0" encoding="UTF-8"?>
<sequence name="GenerateUUID" onError="FaultSeq" trace="disable" xmlns="http://ws.apache.org/ns/synapse">
    <script language="js">
        <![CDATA[
        var result, i, j; 
        result = ''; 
        for(j=0; j<32 ; j++) { 
            if( j== 8 || j == 12|| j == 16|| j == 20) {
                result = result + '-' ; 
            }
            i= Math.floor(Math.random()*16).toString(16).toUpperCase(); 
            result= result + i; 
        } 
       mc.setProperty('UUID',result.toString());       
       ]]>
    </script>
</sequence>